-- users.city — родной город юзера, задаётся на онбординге сразу после
-- аватара, перед шарингом телефона. Дефолтная пустая строка чтобы старые
-- записи не сломались миграцией (они пройдут заново через city-шаг
-- онбординга при следующем заходе, фронт это поймёт по пустому полю).

ALTER TABLE users ADD COLUMN city TEXT NOT NULL DEFAULT '';
