-- Удалить значение из ENUM напрямую Postgres не умеет. Пришлось бы
-- пересоздавать тип и переливать все зависимые колонки. Если очень надо
-- откатить — делать это вручную через ALTER COLUMN ... TYPE TEXT, дроп
-- старого enum'а и cast обратно. В обычной жизни этот down-файл no-op:
-- наличие лишнего value в enum никому не мешает.
SELECT 1;
